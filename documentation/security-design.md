# Security Design

This document explains how AWS account access is secured using IAM users, groups, and roles.
The root account is never used for daily work.
Least privilege access is applied to all users and services.
